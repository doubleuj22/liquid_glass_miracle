name: liquid_glass_bottom_nav
description: A smooth, draggable liquid-glass bottom navigation bar for Flutter.
version: 0.1.0
repository: https://github.com/doubleuj22/liquid_glass_bottom_nav
topics:
  - bottom-navigation
  - liquid-glass
  - navigation-bar
  - animation
  - flutter-ui

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
